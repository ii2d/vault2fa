#!/bin/sh

echo "Running pre-commit formatting & linting (lint-staged)..."
pnpm exec lint-staged || {
  echo "Pre-commit formatting or linting failed."
  exit 1
}

echo "Running unit tests..."
pnpm run test || {
  echo "Unit tests failed."
  exit 1
}
